#数据库配置项
[DB_CONFIG]
dbURL=jdbc:sqlserver://10.1.1.2:1433;DatabaseName=corpzone_test
dbUser=sa
dbPwd=Admin123
#dbURL=${config.db_url}
#dbUser=${config.db_user}
#dbPwd=${config.db_pwd}


#是否使用前置机
[PRE_APP]
isUsePre=${config.use_preapp}
listenPort=${config.listen_port}
loadBalanceType=${config.load_balance_type}

[PRE_INSTANCE1]
IP=${config.pre_instance1_ip}
PORT=${config.pre_instance1_port}

[PRE_INSTANCE2]
IP=${config.pre_instance2_ip}
PORT=${config.pre_instance2_port}

[REDIS_SET]
isUseRedis=${config.use_redis}
CACHENAME=${config.redis_cache_name}
IP=${config.redis_ip}

[MONGO_SET]
isUseMongo=${config.use_mongo}
IP=${config.mongo_ip}
DBNAME=${config.mongo_db_name}

[OTHER]
#是否使用LDAP登录
LDAPCHECK=${config.ldap_check}

[CMBC]
preDay=315
